{
  "name": "lambda-cron-msn-feed",
  "version": "1.0.0",
  "description": "AWS Lambda function to convert RSS feeds into MSN-compliant feeds",
  "main": "index.js",
  "bin": {
    "feed-converter": "./index.js"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node index.js validate",
    "dev": "LOG_LEVEL=debug node index.js",
    "install-deps": "npm install",
    "setup-logs": "mkdir -p logs"
  },
  "keywords": ["aws", "lambda", "rss", "msn", "feed", "converter"],
  "author": "CrackedWeb",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1490.0",
    "xml2js": "^0.6.2",
    "dotenv": "^16.3.1",
    "cheerio": "^1.0.0-rc.12",
    "mysql2": "^3.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
